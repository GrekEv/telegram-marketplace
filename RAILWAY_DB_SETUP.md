# Настройка подключения к PostgreSQL в Railway

## Проблема: "connect ECONNREFUSED ::1:5432"

Это означает, что приложение пытается подключиться к localhost вместо базы данных Railway.

## Решение

### Вариант 1: Использовать DATABASE_URL (РЕКОМЕНДУЕТСЯ)

Railway автоматически создает переменную `DATABASE_URL` при подключении PostgreSQL к вашему сервису.

1. Откройте ваш **Web Service** в Railway
2. Перейдите в **Variables**
3. Найдите переменную `DATABASE_URL` - она должна быть там автоматически
4. Если её нет:
   - Откройте ваш **PostgreSQL** сервис
   - Перейдите в **Variables** или **Connect**
   - Скопируйте **Connection URL** или **DATABASE_URL**
   - Добавьте её в переменные вашего **Web Service**

**Важно:** После обновления кода (с поддержкой DATABASE_URL), эта переменная будет работать автоматически!

---

### Вариант 2: Использовать отдельные переменные

Если DATABASE_URL не работает, используйте шаблонные переменные Railway:

1. Откройте ваш **Web Service** → **Variables**
2. Убедитесь, что PostgreSQL сервис подключен (в правом верхнем углу должен быть значок подключения)
3. Добавьте переменные (замените `Postgres` на имя вашего PostgreSQL сервиса):

```
NODE_ENV=production
PORT=3000
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_NAME=${{Postgres.PGDATABASE}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_BOT_USERNAME=ваш_username_бота
CDN_URL=https://ваш-домен.railway.app/uploads
```

**Важно:** Замените `Postgres` на имя вашего PostgreSQL сервиса, если оно другое!

---

## Как узнать имя вашего PostgreSQL сервиса:

1. В Railway Dashboard посмотрите на список сервисов
2. Найдите PostgreSQL сервис
3. Имя указано под иконкой сервиса (обычно `Postgres`, `PostgreSQL` или другое имя, которое вы дали)

---

## Проверка настроек

После добавления переменных:

1. Railway автоматически перезапустит сервис
2. Проверьте логи деплоя
3. Вы должны увидеть: `📦 Подключение к PostgreSQL успешно`
4. Если всё ещё ошибка - проверьте, что:
   - PostgreSQL сервис запущен
   - Web Service подключен к PostgreSQL (есть значок подключения)
   - Все переменные добавлены правильно

---

## Генерация JWT_SECRET

Если нужно сгенерировать JWT_SECRET:

```bash
openssl rand -hex 32
```

Скопируйте результат и используйте как значение `JWT_SECRET`.

---

## После настройки

После успешного подключения к БД выполните миграцию:

1. Откройте PostgreSQL сервис → **Connect** → **Query**
2. Скопируйте содержимое файла `backend/src/database/schema.sql`
3. Вставьте в Query Editor
4. Нажмите **Run**

Или подключитесь через Railway CLI и выполните:
```bash
railway run npm run migrate
```

---

**После настройки переменных Railway автоматически перезапустит приложение!**

